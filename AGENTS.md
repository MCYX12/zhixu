# 项目协作要求

用户要求每轮任务开始和结束都核对总计划书，并在完成后汇报已实现与未实现功能。

- 开工前阅读 `mac_m4_local_first_ai_v1.md` 和 `docs/implementation-status.md`，把当前任务对应到计划条款及验收条件。
- 完工后再次逐项核对，更新实施状态；涉及范围调整时同步修改总计划，保留实际未完成项。
- 每轮最终回复必须包含本轮新增功能、验证结果，以及参考计划书列出的主要未实现/未验收功能。不要把试点功能、模拟测试或少量冒烟测试称为生产验收完成。
- 默认本地，NAS 暂不实施。联网搜索仅在用户已授权的显式 WEB 模式和服务端账号白名单内使用；云端模型与私有上下文外发保持关闭，除非用户后续明确授权。
- 不输出访问密钥，不覆盖用户文档或会话。延续现有界面风格和本地静态资源，不引入运行时 CDN。
