# 实施状态 · v0.3.1 企业试点

日期：2026-09-22。执行依据：[v2.10 总计划书](../mac_m4_local_first_ai_v1.md)。用户要求企业落地导向、美观前端、暂不使用 NAS，默认本地，本轮已授权手动联网搜索。

## 本轮联网易用性与相关性修复

- 开工核对总计划 §4、§6、§9、§15；按用户反馈将 WEB 改为单输入框，已同步总计划 v2.4 并归档 v2.3。
- 选择联网搜索后，当前问题经 `web_query` 发送给搜索服务；界面显示外发范围，历史和知识库资料不参与。API 仍要求显式公开查询，其他模式不自动联网。
- 复现到 Bing 自动语言/市场参数下的明显无关结果。中文查询固定中文语言与中国市场，去除常见提问修饰词；标题/摘要先过词面相关性筛选，无匹配结果则不交给模型编造。
- “苏州工学院概况”真实请求已取得学校官网、百科、官网学校简介。第一轮 550-token 输出正确涉及学校但被长度截断，随后增加概况简洁回答提示。浏览器已完成单框输入→搜索→学校概况完整回答→3 条来源→保存成功；页面没有捕获到控制台错误。答案逐项语义支持度尚未自动验证。
- 106 项测试通过，Ruff、格式、Node 与 Prettier 检查通过。新增中文市场参数、学校名称归一化、微软/城市结果排除及无相关证据回归。
- 词面过滤不是语义重排，可能遗漏同义表达；未解决所有英文热门排名查询、引擎波动或时效核实。本轮只完成反馈修复，M4/M5/M6 的正式质量、部署和持续稳定性验收继续保留。

## 已实现

- 「知序」自有工作台：深绿侧栏、浅色内容区、AI 工作台/文档知识库/历史会话/系统状态四页，适配桌面与手机。
- 本地 Markdown 渲染和清洗，前端依赖固定且随项目提供，无 CDN。流式问答、停止、复制、来源预览、会话导出、保存失败重试。
- 网页密钥登录换取 8 小时 HttpOnly 会话，CSRF/同源写入检查、HTTPS 下 Secure Cookie、停用账号即时失效。
- 成员/管理员角色、私有/组/全体已认证用户可见文档。管理员不绕过其他成员的私有读取权限。成员与组通过本机 CLI 管理。
- 本机 PDF、DOCX、Markdown、文本代码上传，独立解析进程、文件/正文/数量/原件总量限制。PDF 页码、DOCX 普通表格文字、预览、原件下载、共享修改与删除。失败解析不发布空文档，重名上传不覆盖。
- 原件、片段、ACL、账号会话、聊天历史和操作事件均存于本机 SQLite。聊天历史按账号隔离、版本冲突检测，恢复时再次过滤来源权限。
- 保留 v0.1 的本地 Qwen3.5-9B、Metal 推理、SSE、BM25 检索、ACL、有限队列、超时取消、目录文本入库和备份能力。
- M6：本机 SearXNG、显式公开搜索词、owner 白名单、网页读取与清洗、DNS 固定连接与重定向检查、来源日期/正文/摘要区分、联网引用预览与历史保存、搜索取消及故障提示。
- 搜索依赖独立锁定，本机 8888 端口，当前只启用已验证的 Bing；AUTO/LOCAL/KNOWLEDGE 不自动联网，云端保持关闭。
- 项目新增 AGENTS.md，固定每轮开始/结束核对总计划以及完工汇报已实现与未实现项的协作要求。
- 数据库采用增量迁移；修改前保留 `data/before-enterprise.sqlite3` 本机备份；M6 修改前另存 `data/before-web.sqlite3`，原账号和用户会话保留。

## 验证证据

- **99 项自动测试通过**：既有 81 项与系统代理新增 18 项，包括会话/CSRF/HTTPS Cookie、账号停用、私有及组权限、撤销来源权限、原件下载和删除、上传限制、有效文本 PDF/空 PDF/DOCX 表格、重复文件、会话隔离和版本冲突、伪造引用、静态资源 CSP、备份恢复。
- M6 新增测试覆盖显式查询/模式/账号策略、非公网与混合 DNS、固定 IP/TLS 域名、重定向/大小/压缩限制、正文清洗/发布日期、摘要降级、服务故障/空结果、联网引用历史防伪，以及搜索中断连和并行抓取取消。
- 原有真实 HTTP socket 测试覆盖排队取消及流式断连，上游使用可控后端；自动测试不用于宣称模型答案质量。
- Ruff 检查、格式检查、Node 语法检查、Prettier 检查通过。两条测试依赖上游弃用提示尚存，不影响当前通过。
- 真实浏览器完成：owner 登录 → 上传本机 `examples/workspace-guide.md` → 预览 → 知识库提问 → 本地模型流式回答 → 来源展示 → 保存成功 → 刷新与恢复历史。
- 实际提问「根据工作空间使用指南，演示标识是什么？请简短回答并引用来源。」模型返回「演示标识为 ZX-PILOT-20260913。[1]」。来源可打开；该测试仅证明示例闭环，不等于企业语料质量验收。
- 浏览器检查桌面 1280×720 和手机 390×844；手机会话无横向溢出，导航可进入系统状态。状态页正确显示模型就绪、active=0、waiting=0。
- 登录 Cookie 和已有文档/会话跨页面刷新保留。当前示例指南为 owner 私有资料，原有 PWM 示例保留。

## M6 真实运行验收

- 浏览器完成 owner 登录 → 手动选择联网搜索 → 填写公开搜索词 → 本地 Qwen 流式回答 → 展示 3 个来源 → 保存历史 → 刷新后恢复。
- 来源预览展示正文节选/仅搜索摘要、原站链接、检索时间；无发布日期时显示“未提供”，不伪造时间。
- 手机 390×844 下公开查询面板可用，文档宽度 390，无横向溢出；已恢复桌面视口。
- 重启搜索与 Gateway 后，服务均就绪，原账号/知识库/会话保留。搜索已启用 owner，其他模式仍本地。
- 真搜索+本地模型另一次短样本返回官方文档中的 264 个搜索服务并引用 [2]；全程约 11.54 秒，结束 active=0/waiting=0。原始公开测试记录见 [m6-runtime-validation.json](m6-runtime-validation.json)。这不是持续负载指标，也不承诺该数量永远不变。
- 正文读取失败的来源明确降级为摘要。当前 Bing 可用；其他引擎曾出现连接失败/超时，已从当前配置移除。

## 本轮系统网络适配

### 联网入口与能力声明修复（2026-09-14）

- 核对总计划 §4、§6、§9、§15：继续要求显式 WEB 和公开查询，不因自然语言要求而自动外发。
- 用户截图对应会话处于 AUTO，相关回答无来源；普通聊天缺少运行时能力说明，模型错误声称可以联网。“最近一周”等表达未命中实时性规则。同一会话早前虽有搜索来源，但只是 GitHub 介绍页，不能证明热门排名。
- 已给普通本地聊天注入实际能力说明；补充最近/本周/搜索请求的本地提示；界面标注「智能模式 · 本地」，提示选择联网搜索并填写公开搜索词。
- 103 项测试通过，新增截图原问题、联网能力询问、显式搜索请求和本地能力提示回归；Ruff 与 Node 语法检查通过。模型遵循提示的稳定性和搜索结果质量仍待持续评估。
- 重启 Gateway 后真实 HTTP 验证：截图原问题在 AUTO 下 0.01 秒返回手动联网引导；WEB 以 `GitHub robotics trending weekly repositories` 搜索成功，20.11 秒返回 3 条来源和本地回答，但来源只有 GitHub 介绍/登录页，答案正确说明无法确定排名。本次确认链路可用，同时确认这类查询的搜索相关性尚不满足需求。
- 完工复核：未改变外发权限、NAS 和云端策略；下列 M1～M7 未完成项继续保留，本次不视为企业生产验收。

- 用户要求接入 VPN 后随 VPN 变化。已实现 macOS HTTP/HTTPS/SOCKS 系统代理动态读取、例外规则、回环直连、失败不回退直连，最多缓存 1 秒。
- SearXNG 通过独立适配层逐次选择代理，不修改第三方源码；强制新连接、关闭连接复用及 DNS 缓存，修复 VPN 出口改变时 Bing 地区重定向造成的空正文解析错误。
- 网页抓取采用系统代理，同时保留公网 DNS 校验、IP 固定、原域名 TLS 校验和响应字节上限。真实读取官方 SearXNG 与 Python 网站通过。
- 系统状态增加「搜索网络」字段。切换配置、代理失效、例外、SOCKS、PAC 拒绝、Bing 重定向和取消/大小限制均有回归测试。
- 当前系统代理下真实搜索+网页读取+本地模型返回 HTTP 200，约 13.37 秒，结束 active=0/waiting=0；见 [运行记录](system-network-runtime-validation.json)。本轮前后核对见 [网络适配核对记录](system-network-plan-review.md)。
- 未修改用户 VPN 或系统设置。尚未实际逐一切换 VPN 产品；PAC/WPAD、认证代理、浏览器独立代理、Fake-IP 和域名分流一致性未验收/不支持，详见 README。

## 模型与既有运行记录

Mac mini M4，32GB 统一内存。模型 Qwen3.5-9B Q4_K_M 已通过锁定 SHA256 校验；复用本机 llama.cpp b9716 / db52540f7。模型仅监听 `127.0.0.1:8080`；两个槽位，每槽 8192 tokens，提示缓存上限 1024 MiB，每槽最多 8 个上下文检查点。

v0.1 短样本普通问答首正文约 0.41 秒、资料问答约 1.67 秒；两用户并发约 11.99/11.78 token/s。这些不是本轮重新跑的性能测试，也不代表持续负载或 p95。原始记录见 [runtime-validation.json](runtime-validation.json)，旧状态见[归档](archive/implementation-status-v0.1.md)。

## 尚未完成的企业部署事项

1. 真实企业资料的 50～100 题标注评估、语义混合检索/重排、超限时的预算压缩。目前 BM25 每次读取授权片段，小语料试点可用，大库需优化。
2. SSO、网页新增成员/邀请/密钥轮换、完整账号生命周期；现有成员网页编辑与本机密钥轮换已交付；如需多企业，还须增加独立租户边界。
3. HTTPS 反向代理、可信转发配置、多设备验收、launchd 开机启动、监控告警、长时并发和内存评估。目前仅本机访问、单 Gateway worker。
4. 解析器完整 OS 权限与内存隔离、恶意压缩内容等压力测试；当前独立子进程和超时不等于完整安全沙箱。OCR、复杂版式和结构感知切块尚未实现。
5. 已有可校验备份包、本机独立恢复、自动备份及保留清理；备份加密、业务数据保留、离机恢复演练与审计留存仍待完成。当前本地操作记录不宣称防篡改或满足合规标准。
6. NAS 暂不实施、不阻塞试点；Cloud/DEEP 仍未接入。M6 已有手动搜索闭环，但持续网络可用性、引擎限流、大规模网页质量与正式企业数据留存仍待验收。

引用列表是提供给模型的证据，未自动验证每个论断的语义支持度。权限撤销能阻止继续读取来源，无法回收用户已经看到的答案文字或下载内容。

## 当前启动方式

打开 [本机工作台](http://127.0.0.1:9000/)，使用 `data/owner.key` 中的账号密钥。已有账号和示例无需重新初始化。

```sh
python3 scripts/start_model.py --binary /Users/mac/gemma-lyrics/llama.cpp/build/bin/llama-server
```

其他终端分别运行 `python3 scripts/start_search.py` 和 `uv run python -m local_ai serve`。详细配置、成员管理与备份方法见 [README](../README.md)。

## 本轮完成与计划剩余项

| 阶段 | 当前交付 | 未实现或未验收 |
|---|---|---|
| M0 | 总计划 v2.14、锁定依赖、部署文档、每轮核对要求 | 文档随实施持续更新 |
| M1/M2 | 真模型、API、ACL、队列、取消、本地边界 | 完整长时容量矩阵、自动历史/证据压缩 |
| M3 | 本机文档与会话、引用、上传权限、备份回归 | OCR、复杂结构解析、大规模语料验收 |
| M4 | BM25 基线 | Embedding/Qdrant/RRF/Reranker、50～100 题标注评估 |
| M5 | 自有美观工作台、Cookie 会话、本地操作记录 | SSO、网页成员治理、HTTPS 多设备、launchd、监控与离机恢复 |
| M6 | 本轮手动搜索、正文/摘要/日期、查询隔离与真实联调 | 长期可用性、限流与质量评估；不能宣称生产 SLA |
| M7 | 默认关闭；离线预检工具未启用 | 云模型 API 未获授权，Cloud/DEEP 待后续决定 |
| NAS | 按用户要求暂缓 | 不作为当前交付前置条件 |

本轮逐项复核见 [M6 核对记录](m6-plan-review.md)。

## 本轮文件入口修复

开工与完工核对总计划 §12 部署和 §15 每轮复核。用户通过 file URL 打开 HTML，根路径静态资源被解析成磁盘路径，CSS/JS 和 API 无法正常工作。新增仅在 file 协议执行的默认服务跳转，HTTP/HTTPS 保持不变；README 补充正确入口。

验证：浏览器已打开 http://127.0.0.1:9000/，截图确认图标、配色、布局与原登录状态正常；页面及四项关键资源 HTTP 200，入口脚本三种协议分支、语法与格式检查通过。内置浏览器限制 file URL，未在该浏览器实际执行文件跳转；已直接打开服务页面。

剩余计划不变：精确预算、OCR、语义检索/重排、SSO/HTTPS/监控/开机启动与恢复、多设备及 VPN 长期验收、Cloud/DEEP；NAS 暂缓。

## 本轮 M5 手动一键启动

开工核对总计划 §11、§12、§15，本轮推进部署运维的日常启动入口。已实现项目根目录 `启动知序.command`、启动前健康检查、就绪服务复用、启动锁、失败/超时提示、按策略启动搜索及只读 `--status`。本机模型路径存于不提交的 `config/launcher.local.json`，不自动安装依赖或修改联网设置。

110 项测试通过；新增就绪复用、端口冲突、真实子进程退出及超时清理回归；Ruff 与 zsh 语法检查通过。真实验证：三个现有服务复用成功；确认 active=0/waiting=0 后重启 Gateway，启动器成功拉起，再次执行 `.command --no-open` 仍复用现有服务。模型和搜索没有在本轮停机做冷启动，保留这项验收限制。

完工再次核对：M5 新增手动一键启动，不等于 launchd/开机自启、崩溃自动恢复或企业生产部署完成。其余里程碑未完成项不变，NAS 暂缓。

## 本轮 M1/M2 真实 Token 预算

开工对照总计划 §8 预算与 §15 核对要求。已接入锁定 llama.cpp 的 `/v1/chat/completions/input_tokens`，该接口使用和生成相同的聊天解析与分词路径；读取 `/props` 每槽容量。全量系统提示、历史、RAG/Web 证据均计入，保留输出上限及 32-token 余量。超限返回 413，计数不可用返回 503，均不启动生成；两种情况下释放排队租约。当前及示例配置启用，兼容适配用 Settings 默认关闭；系统状态显示是否启用。

123 项测试通过，新增 13 项预算回归覆盖边界、流式/非流式、异常计数、失败不生成和槽位释放。Ruff、格式、Node 与 Prettier 检查通过。真实运行：Gateway 输入计数 592 与模型 usage.prompt_tokens=592 一致；3000 字符的测试输入计数 12106，超过每槽 8192，0.03 秒返回 413，结束 active=0/waiting=0。证据见 token-budget-runtime-validation.json；仅验证当前固定模型/后端，不宣称其他 tokenizer 通用。

完工对照总计划 v2.6：生成前精确计数已交付；自动历史裁剪、证据压缩、按 tokens 切块及完整容量矩阵仍未完成。M3 OCR、M4 语义检索/重排、M5 企业身份与运维、M6 长期搜索/VPN 质量、M7 Cloud/DEEP 继续保留；NAS 暂缓。

## 2026-09-22：M5 备份与独立恢复

开工核对总计划 §11/§12/§13/§15；交付双击「备份知序.command」、`snapshot`、`verify-backup`、`restore-backup`。包内包含数据库、配置记录、模型版本锁定和 SHA256/数量清单；文件 0600、目录 0700。旧版单文件备份也改为排他创建且从创建时即为私有权限。恢复前检查清单与数据库完整性/外键，只允许新目录，清除恢复副本的旧网页登录会话；不自动覆盖在线库或应用旧外发策略。

130 项测试通过，新增身份/ACL/历史恢复、会话失效、文件损坏、符号链接、已有目标拒绝和清单格式回归。Ruff、格式和 zsh 检查通过。本机真实演练备份 2 份文档、1 份上传原件、1 个会话；原件、会话及可见文档逐项相等，已有 owner 密钥可认证副本、其他用户不能读取私有资料。实际原始密钥未写入报告。证据见 backup-runtime-validation.json。

完工核对总计划 v2.7：本机备份/独立恢复已交付；校验值不等于签名防篡改，备份尚未加密，未配置定时任务、保留轮换或异机恢复。M1/M2 容量与自动压缩、M3 OCR、M4 语义检索/重排评估、M5 身份/HTTPS/运维、M6 长期质量/VPN、M7 Cloud/DEEP 继续保留；NAS 暂缓。

## 2026-09-22：自动备份与状态监测

开工核对总计划 §12/§13/§15。已安装用户 LaunchAgent `com.zhixu.workspace.backup`；登录时及每小时检查，成功备份间隔至少24小时，失败下次重试，已有成功快照损坏/缺失则补做。独立文件锁防止自动任务并发，原子写入私有状态文件。SQLite 备份增加120秒超时检查。保留已有备份，不自动删除。

继续推进运维可见性：系统状态向管理员显示最近自动备份结果和时间，失败/超过25小时未成功会提示；普通成员不获得备份状态。真实 LaunchAgent 已完成首次备份，退出码0，独立 verify_snapshot 校验通过；重启空闲 Gateway 后状态 API 返回成功。134 项测试通过（包含频率、故障重试、缺失补做、路径空格及管理员权限），格式/语法检查通过。

完工复核总计划 v2.8：自动备份及本机状态监测已交付，不代表异机灾备或外部告警；长时间/休眠唤醒场景未实际等待验收。仍待保留轮换、加密、离机恢复、SSO/成员治理、HTTPS、模型/Gateway 守护与完整监控；M3/M4/M6/M7 未完成项继续保留，NAS 暂缓。

## 2026-09-22：自动备份保留与清理

开工核对 §12/§13/§15，用户明确授权自动删除无用备份。默认按 UTC 日期保留近7天每日最新一份、近28天四个滚动7天区间各一份；始终保护本次新成功备份及最新可验证包。仅自动生成、有 automatic 标记、规范名称且完整性校验成功的包参与删除；手动包、旧版未标记包、损坏/未知文件和符号链接保留。

清理与自动备份共用任务锁，仅新备份校验成功后执行，失败不会以清理成功冒充；状态页显示删除数量或清理异常。已部署到现有 LaunchAgent 的执行脚本，无需重装任务。

136 项测试通过，包括模拟36天保留、手动/旧版/损坏/符号链接保护、无有效替代备份拒绝清理。真实连续生成两份自动备份后成功清理当天较旧的1份，最新备份再次校验通过，原有手动和无标记备份保留。详见 retention-runtime-validation.json。Ruff/格式/Node/Prettier 检查通过。

完工复核 v2.9：自动保留轮换已实现；长时运行与大体积备份压力仍待验收，损坏包等例外仍需人工处理。加密、离机恢复、身份治理/HTTPS/服务守护、OCR、语义检索、历史压缩、搜索/VPN长期质量与Cloud/DEEP继续保留；NAS暂缓。

## 2026-09-22：现有成员网页管理

开工对照总计划 §5、§13、§15 与 M5 账号治理。新增系统状态页团队成员区，仅管理员可列出账号及编辑角色/所属组/启用状态。接口不返回密钥或哈希；更新事务重新核验操作者权限，防止身份变化后继续写入。禁止管理员停用自己或自降权，保留至少一名有效管理员。变更清除目标网页登录会话，停用账号不能继续用旧密钥登录；恢复账号不会恢复旧 Cookie。

138 项测试通过，覆盖普通成员越权、Cookie CSRF、列表字段、停用/恢复、所属组、会话撤销、自保护和非法输入。Ruff、Node 语法与 Prettier 通过。Gateway 已重启，真实管理员 GET /api/members 返回200和2个现有账号，仅包含id/role/active/groups；没有修改真实账号权限。网页编辑流程的真实浏览器交互尚未实测，不将静态检查视作浏览器验收。

完工复核总计划 v2.10：现有成员网页编辑已实现；网页新增账号、邀请、密钥轮换、SSO及完整生命周期仍未完成。M1/M2 长时容量/自动压缩，M3 OCR，M4 语义检索/重排，M5 HTTPS/服务守护/离机恢复，M6 长期质量/VPN，M7 Cloud/DEEP继续保留。NAS暂缓。

## 2026-09-22：成员交互验收与本机密钥轮换

开工核对总计划 §5/§13/§15，本轮对应 M5 权限生命周期：验收成员变更能保存、旧凭据失效、撤销组权限后资料不可访问。新增本机 `rotate-key USER --key-file PATH`，新密钥以 0600 排他创建，不覆盖已有文件、不输出密钥；写入并同步成功后事务撤销该账号全部旧密钥和网页登录会话，写入本地操作记录。写入失败保留旧凭据。成员变更改为工作台风格确认弹窗。

143 项测试通过，Ruff 通过；新增轮换成功/已有目标/写盘失败/停用账号回归，以及移除组后文档预览和下载拒绝回归。Node 语法与前端格式检查通过。独立数据库真实浏览器验证成员保存、停用状态和成功提示；独立 CLI 轮换后旧密钥 HTTP 401、新密钥 HTTP 200，文件权限 0600。未修改正式账号。证据见 member-governance-runtime-validation.json。此验证不代表多设备或生产生命周期验收。

完工逐项核对总计划 v2.11：网页现有成员交互和本机密钥轮换已交付；网页新增/邀请/密钥轮换、SSO、完整生命周期未完成。M1/M2 长时容量和自动压缩，M3 OCR/复杂解析，M4 向量检索/重排与标注评估，M5 HTTPS/服务守护/备份加密及离机恢复，M6 长期搜索质量/VPN兼容验收，M7 Cloud/DEEP 仍保留；NAS 暂缓。

## 2026-09-22：云端接入预检（历史误解，已由下节更正）

开工核对 §4/§6/§8/§9/§15，对应 M7。用户已明确授权开启云端；已询问服务商/模型及外发范围，尚未收到答复，不推断允许上传企业文档或历史。当前实现 cloud-check 离线预检与 config/cloud.example.toml 模板，独立本机配置加入忽略规则。校验 HTTPS 地址结构（不做 DNS 公网验证）、白名单、当前问题范围、输出上限、币种/价格/预算、当前用户持有的 0600 非符号链接密钥文件。报告不含密钥、不启用推理、不调用外部服务、不创建业务数据库。

验证：161 项测试通过（新增18项），Ruff通过。覆盖无效地址、空白名单、非法预算/无穷/NaN、文件权限/符号链接、内联密钥和上下文范围。实际运行模板检查返回退出码1并准确列出缺失配置。仅离线验证，不是云端连接、计费或模型验收。

完工核对 v2.12：云端尚未实际开启，待服务商、模型、密钥、外发范围和每日费用上限落实；云端调用适配、网页入口、运行时预算扣减、取消/超时与真实调用验收尚未交付。M1/M2长时容量/压缩、M3 OCR、M4语义检索/重排、M5身份治理/HTTPS/服务守护/加密与离机恢复、M6长期搜索/VPN、M7 DEEP继续保留；NAS暂缓。

## 2026-09-22：更正范围，推进 M5 远程访问

用户明确云端是指通过网络访问工作台，不是云模型 API；上一轮“云推理已获授权”的记录无效。开工核对 §4/§12/§13/§15，本轮按 M5 HTTPS/可信转发推进。模型、文档、会话继续在 Mac；云模型调用保持禁用。

新增可选 public_origin 配置，对公网域名要求 HTTPS 并拒绝未知 Host；保留本机入口。CLI 显式配置可信代理 IP，不继承环境变量扩大代理信任，拒绝通配符。HTTPS 会话复用原有 Secure Cookie、CSRF/同源检查及 ACL。提供正式命名隧道配置模板和 docs/remote-access.md；已询问域名/服务器资源，尚未收到答复，未对外发布服务。

验证：新增8项自动测试通过，覆盖代理信任、伪造协议头、HTTPS登录与CSRF、Host/本机入口及非法配置。全套169项测试通过，Ruff/格式检查通过。均为本地测试，尚未验收真实证书、隧道、异网浏览器、SSE及上传下载。没有修改正式公网配置或重启现有服务。

完工核对 v2.13：远程部署应用适配已完成，实际入口待域名/服务器或私网方案确定；登录限流/入口访问控制、服务守护和多设备/长时验收仍待推进。M1/M2容量与压缩、M3 OCR、M4语义检索、M5 SSO/备份加密及离机恢复、M6长期搜索/VPN、M7 Cloud/DEEP仍未完成；NAS暂缓。

## 2026-09-22：仅域名部署准备

开工及完工核对总计划 §12/§13/§15，用户确认仅有域名、没有云服务器。按 M5 正式命名隧道方案准备，已安装 cloudflared 2026.6.1；真实 CLI 验证模板语法通过，目标示例域名匹配本机9000，未知域名匹配404。未修改业务代码，本轮未重复运行全套测试；上一轮169项通过仍仅代表此前代码回归。

尚待具体域名、DNS平台与Cloudflare账号接入，未登录、创建隧道、修改DNS或启动外网入口。生产证书/公网访问/真实SSE/异网登录仍未验收，服务守护、访问控制/限流与长期可用性继续保留。其他M1/M2容量和压缩、M3 OCR、M4语义检索、M5 SSO/备份加密和离机恢复、M6搜索/VPN长期验收、M7未完成项不变；云模型API关闭，NAS暂缓。

## 公网部署状态核对（用户报告已在 Cloudflare 部署）

开工/完工对照总计划 §12/§13/§15。用户报告已完成 Cloudflare 部署；本轮只读检查确认本机 cloudflared 进程存在、本地 /health 返回正常。默认解析器仍返回阿里云 NS，指定 1.1.1.1 的查询返回 jocelyn.ns.cloudflare.com 和 dax.ns.cloudflare.com；指定8.8.8.8查询子域名返回198.18.0.4（非公网地址，可能为本机VPN/Fake-IP，不能作为真实公网解析验收）。本机 curl 访问 ai.zhixingmoyun.com 和根域均报告解析失败；按该合成IP测试也未完成TLS。进程存在不等于隧道已就绪。

已询问实际部署网址与部署类型，待确认后继续诊断。本轮未修改正式配置或DNS，没有发送账号密钥至外部，也未重复测试业务代码。新增功能：无；验证：本地健康正常，公网访问尚未确认。仍待实际网址/DNS/HTTPS/隧道/外网登录与流式聊天验收，M5服务守护/入口控制、M3 OCR、M4语义检索及其余计划未完成项保留。

## 根域名公网闭环验证

用户明确实际入口 https://zhixingmoyun.com/。按 §12/§13/§15 继续验证：本机直接curl不能解析，但显式使用系统HTTP代理127.0.0.1:7897后HTTPS返回200，页面内容与本机index.html的SHA256完全一致，三项静态资源均200。未登录接口401；使用已有账号创建本次测试会话，登录200、Cookie含Secure、认证后接口200、缺少CSRF的写请求403。公开短测试问题由local模式返回200，收到5个流式块和DONE、无流错误；随后注销本次测试会话200。未读出私有文档或历史，未保存测试对话，未打印密钥。

证据：public-access-runtime-validation.json。此为真实公网HTTP链路冒烟，经过当前Mac的系统代理；不是独立手机网络/浏览器交互或持续负载验收。正式配置public_origin尚未设置，需后续启用域名限制；尚待访问限流、服务守护、上传下载/取消与多设备验收。本轮没有业务代码变更，未重跑全套单元测试；原有OCR、语义检索、SSO、备份加密/离机恢复等计划未完成项保留，云模型关闭，NAS暂缓。

## 公网域名限制与登录防刷

开工对照 §5/§12/§13/§15，M5 本轮验收为公网HTTPS登录继续可用、本机入口保留、未知Host/公网明文拒绝、登录超限可恢复。已启用正式public_origin和127.0.0.1可信代理；新增网页登录每来源60秒10次限制，成功失败均计入，429附Retry-After，状态最多4096来源，容量满新来源拒绝。仅使用ASGI已经处理的来源地址，不直接信任任意转发头。范围仅POST /api/session，不代表全部API鉴权限流或DDoS防护；共享出口可能共享配额，重启清零。

173项测试通过，Ruff/格式检查通过。新增限制到期、容量上限、伪造头拒绝绕过及可信代理来源隔离测试。备份配置并确认active=0/waiting=0后重启Gateway，模型/搜索继续复用。真实HTTPS验证：首页200、登录200、Secure Cookie存在、注销200；本机首页200、未知Host400、公网Host明文400。证据见public-hardening-runtime-validation.json。未在正式公网连续撞击账号验证限流，限流由隔离测试验证。

完工复核v2.14：本轮域名限制和网页登录防刷已部署。仍待Bearer API失败鉴权限流、边缘访问控制、服务守护、独立手机异网/上传下载/取消与长期运行验收。M1/M2容量/压缩、M3 OCR、M4语义检索/标注评估、M5 SSO/备份加密与离机恢复、M6长期搜索/VPN及M7未完成项继续保留，云模型关闭，NAS暂缓。
