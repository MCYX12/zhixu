# M5 一键启动前后核对

2026-09-14。开工对照总计划 v2.4 §11/§12/§15 与实施状态，选择手动启动入口作为本轮交付。完工总计划更新至 v2.5，旧版归档。

交付：双击启动文件、健康探测、顺序启动、复用现有进程、文件锁、日志路径、超时清理本次进程、只读状态检查；搜索严格遵守原配置，所有服务保持默认回环监听。无新增外发、云端或 NAS。

验证：110 项测试通过，Ruff/zsh 检查通过。实际复用三个服务；Gateway 空闲后停机，由新启动器重新启动，再次执行脚本正常复用。健康检查仅验证本机进程，不证明外部搜索质量。模型与搜索本轮未冷启动验收。

尚未实现：M1/M2 精确预算与容量矩阵；M3 OCR/结构解析；M4 语义混合检索与评估；M5 SSO、网页成员治理、HTTPS、多设备、launchd、监控和离机恢复；M6 长期质量/VPN 兼容；M7 Cloud/费用/DEEP。NAS 暂缓。
