# STM32 TIM1 PWM 排查示例

这是用于系统验证的示例资料，不替代芯片参考手册或电路测量。

## 示例板的已知配置

实验板 A 使用 STM32F103ZET6，TIM1 通道 1 的输出引脚配置为 PA8。该板实验记录编号为 DEMO-PWM-20260913。

## 示例检查顺序

本示例要求先确认定时器和 GPIO 时钟，再核对引脚复用配置；随后检查计数器使能、通道输出使能以及高级定时器主输出使能 MOE。最后检查自动重装载和比较值，确认占空比没有被配置为持续低电平。

该示例没有提供真实板子的示波器数据，因此不能仅凭本文确定实际故障原因。
